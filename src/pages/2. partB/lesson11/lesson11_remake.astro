---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `<!-- 🛠️ Project 11: Functions & Click Events -->
<button id="counterBtn">Clicks: 0</button>

<script>
  let count = 0;

  // Step 1: Write a function incrementCount() that increases count by 1


  // Step 2: Update the button's textContent inside the function


  // Step 3: Attach an 'click' event listener to counterBtn

<\/script>
`;

const level4Lessons = [
    {
        "id": 1,
        "title": "Interactive HTML Page Structure",
        "url": "/2. partB/lesson1/lesson1_remake.html"
    },
    {
        "id": 2,
        "title": "Typography & Heading Hierarchy",
        "url": "/2. partB/lesson2/lesson2_remake.html"
    },
    {
        "id": 3,
        "title": "Interactive Links & Dynamic Media",
        "url": "/2. partB/lesson3/lesson3_remake.html"
    },
    {
        "id": 4,
        "title": "Lists & Dropdown Drawer Navigation",
        "url": "/2. partB/lesson4/lesson4_remake.html"
    },
    {
        "id": 5,
        "title": "Semantic HTML Web Architecture",
        "url": "/2. partB/lesson5/lesson5_remake.html"
    },
    {
        "id": 6,
        "title": "CSS Colors, Backgrounds & Borders",
        "url": "/2. partB/lesson6/lesson6_remake.html"
    },
    {
        "id": 7,
        "title": "The CSS Box Model & Spacing",
        "url": "/2. partB/lesson7/lesson7_remake.html"
    },
    {
        "id": 8,
        "title": "Modern Flexbox Layout Mastery",
        "url": "/2. partB/lesson8/lesson8_remake.html"
    },
    {
        "id": 9,
        "title": "Responsive CSS Grid Architecture",
        "url": "/2. partB/lesson9/lesson9_remake.html"
    },
    {
        "id": 10,
        "title": "JavaScript Variables & Data Types",
        "url": "/2. partB/lesson10/lesson10_remake.html"
    },
    {
        "id": 11,
        "title": "Functions & Interactive Click Events",
        "url": "/2. partB/lesson11/lesson11_remake.html"
    },
    {
        "id": 12,
        "title": "DOM Manipulation & Dynamic Elements",
        "url": "/2. partB/lesson12/lesson12_remake.html"
    },
    {
        "id": 13,
        "title": "Interactive Forms & Input Validation",
        "url": "/2. partB/lesson13/lesson13_remake.html"
    },
    {
        "id": 14,
        "title": "Asynchronous JavaScript & REST APIs",
        "url": "/2. partB/lesson14/lesson14_remake.html"
    },
    {
        "id": 15,
        "title": "Capstone: Full Interactive Web Application",
        "url": "/2. partB/lesson15/lesson15_remake.html"
    }
];
---

<LessonLayout
    title="Project 11: Functions & Interactive Click Events | Level 4 DOM Dojo"
    description="Write reusable functions and attach addEventListener to create responsive user actions."
    levelTag="Level 4 • DOM Dojo"
    lessonNum={11}
    lessonTitle="Functions & Interactive Click Events"
    xpAmount={100}
    completionKey="partB_lesson11_remake_complete"
    nextLessonUrl="/2. partB/lesson12/lesson12_remake.html"
    hubUrl="/2. partB/hub.html"
    hubTitle="Level 4 Hub"
    hubIcon="⚔️"
    fileTab="index.html"
    starterCode={starterCode}
    mode="html"
    jumpLessons={level4Lessons}
    certificateUrl="/2. partB/certificate.html"
    trackPrefix="partB"
>
    <!-- Intro Slot -->
    <p class="lesson-subtitle" slot="intro">
        Write reusable functions and attach addEventListener to create responsive user actions.
    </p>

    <!-- Concept Slot (Left Pane) -->
    <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
        
        <!-- Core Concept & Analogy -->
        <article class="concept-card">
            <h3><span>💡</span> Core Concept &amp; Mental Model</h3>
            <div class="analogy-callout">
                <strong>Real-World Analogy:</strong> Functions are recipes with instructions, and event listeners are watchful sensors waiting for a physical button press to trigger the recipe.
            </div>
        </article>

        
        <!-- Event Listener Sensor Breakdown -->
        <article class="concept-card">
            <h3><span>🖱️</span> The Event Listener Sensor</h3>
            <div class="intro-highlight">
                <code>button.addEventListener('click', myFunction);</code>
                <p style="margin: 6px 0 0 0; font-size: 0.82rem; color: #1e3a8a;">Listens for user interaction in real time and immediately executes your assigned function handler.</p>
            </div>
        </article>


        <!-- Working Reference Code & Target Result -->
        <article class="concept-card">
            <h3><span>💻</span> Reference Code Pattern</h3>
            <p style="color: #475569; font-size: 0.88rem; margin: 0 0 8px 0;">How this architecture looks in real-world code:</p>
            <div class="code-snippet-box">
                <button class="snippet-copy-btn" onclick="navigator.clipboard.writeText(document.getElementById('refCodeBox_11')?.innerText || '')">📋 Copy</button>
                <pre><code id="refCodeBox_11">&lt;button id="counterBtn"&gt;Clicks: 0&lt;/button&gt;
&lt;script&gt;
  const btn = document.getElementById('counterBtn');
  let count = 0;
  function incrementCount() &#123;
    count++;
    btn.textContent = 'Clicks: ' + count;
  &#125;
  btn.addEventListener('click', incrementCount);
&lt;/script&gt;</code></pre>
            </div>
        </article>

        <!-- Task Instructions Card -->
        <article class="concept-card" style="border-left: 4px solid #2563eb;">
            <h3><span>🎯</span> Your Hands-On Coding Goal</h3>
            <p style="color: #475569; font-size: 0.90rem; line-height: 1.6; margin: 0 0 10px 0;">
                In the code editor on the right pane:
            </p>
            <ol style="color: #334155; font-size: 0.90rem; line-height: 1.7; padding-left: 20px; margin: 0;">
                <li>Create a function <code>incrementCount()</code>.</li>
                <li>Increment <code>count</code> and update button text.</li>
                <li>Attach <code>addEventListener("click", ...)</code> to <code>#counterBtn</code>.</li>
            </ol>
        </article>

    </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
    import { DojoEngine } from '../../../scripts/dojo';

    function initLesson() {
        const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
        const lineNumbers = document.getElementById('ideLineNumbers');
        const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
        const previewIframe = document.getElementById('livePreviewIframe') as HTMLIFrameElement | null;
        const checkBtn = document.getElementById('checkAnswerBtn');
        const resetBtn = document.getElementById('resetEditorBtn');

        if (!editor) return;

        const DRAFT_KEY = 'novicodes_draft_partB_lesson11';
        const DEFAULT_STARTER = `<!-- 🛠️ Project 11: Functions & Click Events -->
<button id="counterBtn">Clicks: 0</button>

<script>
  let count = 0;

  // Step 1: Write a function incrementCount() that increases count by 1


  // Step 2: Update the button's textContent inside the function


  // Step 3: Attach an 'click' event listener to counterBtn

<\/script>
`;

        // 1. Initialize Real-Time Conceptual Checklist
        DojoEngine.initChecklist([
            { 
                id: 'func', 
                label: 'Define a function <code>incrementCount</code>', 
                fn: (code) => /function\s+incrementCount\s*\(/i.test(c) || /const\s+incrementCount\s*=\s*\(/i.test(code),
            },
            { 
                id: 'listener', 
                label: 'Attach <code>addEventListener("click", ...)</code> to counterBtn', 
                fn: (code) => /addEventListener\(\s*['"]click['"]\s*,\s*incrementCount\s*\)/i.test(c) || /addEventListener\(\s*['"]click['"]\s*,\s*\(\)\s*=>/i.test(code)
            }
        ], {
            containerId: 'taskChecklist',
            submitBtnId: 'checkAnswerBtn',
            nextBtnId: 'nextLessonBtn',
            mode: 'html'
        });

        // 2. Line numbers and scroll sync
        function syncScroll() {
            if (lineNumbersInner && editor) {
                lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
            }
        }

        function updateEditor() {
            if (!editor || !lineNumbersInner) return;
            const lines = editor.value.split('\n');
            const lineCount = Math.max(lines.length, 1);
            lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
            syncScroll();

            // Render live preview instantly via srcdoc
            if (previewIframe) {
                previewIframe.srcdoc = editor.value;
            }

            // Real-Time Intelligent Linter
            DojoEngine.lint(editor.value, 'html');

            // Real-Time Checklist
            DojoEngine.runChecklist(editor.value);
        }

        // 3. Draft Auto-Save & Crash Resilience
        DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

        editor.addEventListener('scroll', syncScroll);

        // Tab indentation key support
        editor.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                e.preventDefault();
                const start = editor.selectionStart;
                const end = editor.selectionEnd;
                editor.value = editor.value.substring(0, start) + '  ' + editor.value.substring(end);
                editor.selectionStart = editor.selectionEnd = start + 2;
                updateEditor();
            }
        });

        // 4. Safe Reset Button Handler
        if (resetBtn) {
            DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);
        }

        // 5. Check & Verify Code Action with Empty & Diagnostics Protection
        if (checkBtn) {
            checkBtn.addEventListener('click', () => {
                const code = editor.value;

                // Empty or Whitespace-only Submission Protection
                if (DojoEngine.isEmpty(code)) {
                    DojoEngine.showLint('Your code editor is empty! Write your code before verifying.', 'error', 'Follow the step-by-step instructions in the left pane.');
                    return;
                }

                // Check for blocking syntax errors from Linter
                const problems = DojoEngine.lint(code, 'html');
                const hasErrors = problems.some(p => p.severity === 'error');
                if (hasErrors) {
                    return; // Block submission while syntax errors exist
                }

                const result = DojoEngine.runChecklist(code);

                if (result.allPassed) {
                    DojoEngine.clearLint();
                    DojoEngine.celebrateCompletion({
                        lessonTitle: 'Lesson 11: Functions & Interactive Click Events',
                        xp: 100,
                        completionKey: 'partB_lesson11_remake_complete',
                        nextUrl: '/2. partB/lesson12/lesson12_remake.html',
                        customMessage: 'You have mastered this project challenge!'
                    });
                } else {
                    DojoEngine.showLint('Project requirements incomplete', 'warning', 'Check the task checklist below to see remaining requirements.');
                }
            });
        }

        // Initial render
        updateEditor();
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLesson);
    } else {
        initLesson();
    }
</script>
