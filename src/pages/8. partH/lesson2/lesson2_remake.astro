---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `// Lesson 2: Global useAuth() Context & Session Hook!

const AuthContext = React.createContext(null);

function AuthProvider({ children }) {
 const [user, setUser] = React.useState({ username: "Alex", role: "developer" });

 const logout = () => setUser(null);

 return (
 // STEP 1: Provide value={{ user, logout }} to AuthContext.Provider:
 // (Hint: <AuthContext.Provider value={{ user, logout }}>{children}</AuthContext.Provider>)
 <div className="auth-provider-wrap">
 {children}
 </div>
 );
}

function UserBadge() {
 const { user, logout } = React.useContext(AuthContext) || { user: { username: "Alex", role: "developer" }, logout: () => {} };
 return (
 <div style={{ padding: '20px', fontFamily: 'sans-serif', maxWidth: '400px', margin: '0 auto', textAlign: 'center' }}>
 <h2> Global Session Header</h2>
 {user ? (
 <div style={{ padding: '12px', background: '#f8fafc', borderRadius: '10px', border: '1px solid #e2e8f0' }}>
 <p style={{ margin: '0 0 8px' }}>Active User: <strong>{user.username}</strong> ({user.role})</p>
 <button onClick={logout} style={{ padding: '6px 12px', background: '#ef4444', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }}>
 Log Out
 </button>
 </div>
 ) : (
 <p style={{ color: '#64748b' }}>No active session.</p>
 )}
 </div>
 );
}

function App() {
 return (
 <AuthProvider>
 <UserBadge />
 </AuthProvider>
 );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
`;

const jumpLessons = [
 {
 "id": 1,
 "title": "Managing Login Tokens in State",
 "url": "/8. partH/lesson1/lesson1_remake.html"
 },
 {
 "id": 2,
 "title": "Global Session Provider (AuthContext)",
 "url": "/8. partH/lesson2/lesson2_remake.html"
 },
 {
 "id": 3,
 "title": "Locking Private Pages (Route Guards)",
 "url": "/8. partH/lesson3/lesson3_remake.html"
 },
 {
 "id": 4,
 "title": "Managing Team Member Lists (Clean State)",
 "url": "/8. partH/lesson4/lesson4_remake.html"
 },
 {
 "id": 5,
 "title": "Role Gates (Admin vs Member Views)",
 "url": "/8. partH/lesson5/lesson5_remake.html"
 },
 {
 "id": 6,
 "title": "Final Project: Secure Team Workspace",
 "url": "/8. partH/lesson6/lesson6_remake.html"
 }
];
---

<LessonLayout
 title="Global Session Provider (AuthContext) | Level 9 • React Auth & Access Control"
 description="Share the logged-in user info across every component without messy prop-drilling."
 levelTag="Level 9 • React Auth & Access Control"
 lessonNum={2}
 lessonTitle="Global Session Provider (AuthContext)"
 xpAmount={250}
 completionKey="partH_lesson2_remake_complete"
 nextLessonUrl="/8. partH/lesson3/lesson3_remake.html"
 hubUrl="/8. partH/hub.html"
 hubTitle="Level 9 Hub"
 hubIcon="️"
 fileTab="Auth.jsx"
 mode="react"
 jumpLessons={jumpLessons}
 certificateUrl="/8. partH/certificate.html"
 trackPrefix="partH"
 starterCode={starterCode}
>
 <!-- Intro Slot -->
 <p class="lesson-subtitle" slot="intro">
 Share the logged-in user info across every component without messy prop-drilling.
 </p>

 <!-- Concept Slot (Left Pane) -->
 <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
 <!-- Unified Mission Card -->
 <article class="concept-card mission-card">
 <div class="mission-header">
 <h3><span></span> Your Mission</h3>
 <span class="checklist-counter" id="checklistCounter">0 / 2</span>
 </div>
 <div class="target-output-preview" style="background: #f1f5f9; border-radius: 8px; padding: 10px 12px; margin: 8px 0 12px 0; border: 1px dashed #94a3b8; font-size: 0.82rem;">
 <strong style="color: #334155;">️ Target Goal:</strong>
 <div style="color: #475569; margin-top: 3px;">Build an AuthContext provider that shares the user session and logout function everywhere!</div>
 </div>
 <p class="mission-subtitle">Follow the step-by-step instructions below in the editor:</p>
 <div id="taskChecklist"></div>
 </article>

 <!-- Analogy Explainer Card -->
 <article class="concept-card">
 <h3><span></span> Core Concept &amp; Mental Model</h3>
 <p>Design a global React Authentication Context providing reactive user profile session state and a logout handler to all components.</p>
 <div class="analogy-callout">
 <strong>Real-World Analogy:</strong> React Context is like a building-wide PA broadcast system : every room immediately knows the announcements without passing messages door-to-door.
 </div>
 </article>
 </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
 import { DojoEngine } from '../../../scripts/dojo';

 function initLesson() {
 const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
 const lineNumbers = document.getElementById('ideLineNumbers');
 const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
 const previewIframe = document.getElementById('livePreviewIframe') as HTMLIFrameElement | null;
 const checkBtn = document.getElementById('checkAnswerBtn');
 const resetBtn = document.getElementById('resetEditorBtn');

 if (!editor) return;

 const DRAFT_KEY = 'novicodes_draft_partH_lesson2';
 const DEFAULT_STARTER = `// Lesson 2: Global useAuth() Context & Session Hook!

const AuthContext = React.createContext(null);

function AuthProvider({ children }) {
 const [user, setUser] = React.useState({ username: "Alex", role: "developer" });

 const logout = () => setUser(null);

 return (
 // STEP 1: Provide value={{ user, logout }} to AuthContext.Provider:
 // (Hint: <AuthContext.Provider value={{ user, logout }}>{children}</AuthContext.Provider>)
 <div className="auth-provider-wrap">
 {children}
 </div>
 );
}

function UserBadge() {
 const { user, logout } = React.useContext(AuthContext) || { user: { username: "Alex", role: "developer" }, logout: () => {} };
 return (
 <div style={{ padding: '20px', fontFamily: 'sans-serif', maxWidth: '400px', margin: '0 auto', textAlign: 'center' }}>
 <h2> Global Session Header</h2>
 {user ? (
 <div style={{ padding: '12px', background: '#f8fafc', borderRadius: '10px', border: '1px solid #e2e8f0' }}>
 <p style={{ margin: '0 0 8px' }}>Active User: <strong>{user.username}</strong> ({user.role})</p>
 <button onClick={logout} style={{ padding: '6px 12px', background: '#ef4444', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }}>
 Log Out
 </button>
 </div>
 ) : (
 <p style={{ color: '#64748b' }}>No active session.</p>
 )}
 </div>
 );
}

function App() {
 return (
 <AuthProvider>
 <UserBadge />
 </AuthProvider>
 );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
`;

 // 1. Initialize Real-Time Conceptual Checklist
 DojoEngine.initChecklist([
 { id: 'provider', label: 'Wrap with <code>&lt;AuthContext.Provider value=...&gt;</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /<AuthContext\.Provider/i.test(c); } },
 { id: 'value', label: 'Pass <code>value={{ user, logout }}</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /value[\s\n]*=[\s\n]*\{[\s\n]*\{[\s\n]*user[\s\n]*,[\s\n]*logout[\s\n]*\}[\s\n]*\}/i.test(c); } }
 ], {
 containerId: 'taskChecklist',
 mode: 'javascript'
 });

 // 2. Line numbers & scroll sync
 function syncScroll() {
 if (lineNumbersInner && editor) {
 lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
 }
 }

 function updateEditor() {
 if (!editor || !lineNumbersInner) return;
 const lines = editor.value.split('\n');
 const lineCount = Math.max(lines.length, 1);
 lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
 syncScroll();

 // Render live React JSX preview inside iframe
 if (previewIframe) {
 const code = editor.value;
 const htmlDoc = `<!DOCTYPE html>
<html>
<head>
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <script src="https://unpkg.com/react@18/umd/react.development.js"><\/script>
 <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"><\/script>
 <script src="https://unpkg.com/@babel/standalone/babel.min.js"><\/script>
 <style>
 body { margin: 0; padding: 12px; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #ffffff; color: #0f172a; }
 </style>
</head>
<body>
 <div id="root"></div>
 <script type="text/babel">
 try {
 ${code}
 } catch (err) {
 document.getElementById('root').innerHTML = '<div style="color:#ef4444; background:#fef2f2; border:1px solid #fecaca; padding:12px; border-radius:8px; font-size:13px; font-family:monospace;"><b>️ Render Error:</b> ' + err.message + '</div>';
 }
 <\/script>
</body>
</html>`;
 previewIframe.srcdoc = htmlDoc;
 }

 // Real-Time Syntax & Diagnostics
 DojoEngine.lint(editor.value, 'javascript');

 // Real-Time Checklist Verification
 DojoEngine.runChecklist(editor.value);
 }

 // 3. Draft Auto-Save & Crash Resilience
 DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

 editor.addEventListener('scroll', syncScroll);

 editor.addEventListener('keydown', (e) => {
 if (e.key === 'Tab') {
 e.preventDefault();
 const start = editor.selectionStart;
 const end = editor.selectionEnd;
 editor.value = editor.value.substring(0, start) + ' ' + editor.value.substring(end);
 editor.selectionStart = editor.selectionEnd = start + 2;
 updateEditor();
 }
 });

 // 4. Safe Reset Button Handler
 if (resetBtn) {
 DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, () => {
 updateEditor();
 });
 }

 // 5. Check & Verify Code Action with Diagnostics
 if (checkBtn) {
 checkBtn.addEventListener('click', () => {
 DojoEngine.verifySubmission(editor.value, {
 lessonTitle: 'Global useAuth() Context & Session Hook',
 xp: 250,
 completionKey: 'partH_lesson2_remake_complete',
 nextUrl: '/8. partH/lesson3/lesson3_remake.html',
 mode: 'javascript'
 });
 });
 }

 // Initial Paint
 updateEditor();
 }

 if (document.readyState === 'loading') {
 document.addEventListener('DOMContentLoaded', initLesson);
 } else {
 initLesson();
 }
</script>
