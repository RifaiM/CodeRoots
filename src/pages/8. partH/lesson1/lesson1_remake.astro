---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `// 🛡️ Level 9 Lesson 1: Stateless JWT Login & Token Storage!

function LoginPortal() {
  const [token, setToken] = React.useState(null);

  const handleLogin = (e) => {
    e.preventDefault();
    const fakeToken = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.demo";
    // 👉 STEP 1: Persist token to localStorage using localStorage.setItem("jwt_token", fakeToken):
    // (Hint: localStorage.setItem("jwt_token", fakeToken);)
    

    // 👉 STEP 2: Update local state with setToken(fakeToken):
    
  };

  return (
    <div style={{ padding: '24px', fontFamily: 'sans-serif', maxWidth: '400px', margin: '0 auto', background: '#ffffff', borderRadius: '16px', border: '1px solid #e2e8f0' }}>
      <h2>🔐 Secure Member Login</h2>
      {token ? (
        <div style={{ padding: '16px', background: '#dcfce7', color: '#15803d', borderRadius: '10px' }}>
          <strong>✅ Authenticated!</strong>
          <p style={{ fontSize: '0.80rem', margin: '6px 0 0', wordBreak: 'break-all' }}>Bearer: {token}</p>
        </div>
      ) : (
        <form onSubmit={handleLogin} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
          <input defaultValue="dev_user" placeholder="Username" style={{ padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }} />
          <input type="password" defaultValue="secret123" placeholder="Password" style={{ padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }} />
          <button type="submit" style={{ padding: '10px', background: '#4f46e5', color: 'white', border: 'none', borderRadius: '8px', fontWeight: 700, cursor: 'pointer' }}>
            Authenticate with JWT
          </button>
        </form>
      )}
    </div>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<LoginPortal />);
`;

const jumpLessons = [
    {
        "id": 1,
        "title": "Auth Token State & Client Session Storage",
        "url": "/8. partH/lesson1/lesson1_remake.html"
    },
    {
        "id": 2,
        "title": "Global AuthContext & useAuth() Hook",
        "url": "/8. partH/lesson2/lesson2_remake.html"
    },
    {
        "id": 3,
        "title": "Protected Route Guards & Redirects",
        "url": "/8. partH/lesson3/lesson3_remake.html"
    },
    {
        "id": 4,
        "title": "User Directory State & Immutable Mutations",
        "url": "/8. partH/lesson4/lesson4_remake.html"
    },
    {
        "id": 5,
        "title": "Role-Based Access Control (RBAC) UI Gates",
        "url": "/8. partH/lesson5/lesson5_remake.html"
    },
    {
        "id": 6,
        "title": "Capstone: Secure Workspace with Role Guards",
        "url": "/8. partH/lesson6/lesson6_remake.html"
    }
];
---

<LessonLayout
    title="Auth Token State & Client Session Storage | Level 9 • React Auth & Access Control"
    description="Manage authentication tokens in React state and handle login/logout transitions."
    levelTag="Level 9 • React Auth & Access Control"
    lessonNum={1}
    lessonTitle="Auth Token State & Client Session Storage"
    xpAmount={250}
    completionKey="partH_lesson1_remake_complete"
    nextLessonUrl="/8. partH/lesson2/lesson2_remake.html"
    hubUrl="/8. partH/hub.html"
    hubTitle="Level 9 Hub"
    hubIcon="🛡️"
    fileTab="Auth.jsx"
    mode="react"
    jumpLessons={jumpLessons}
    certificateUrl="/8. partH/certificate.html"
    trackPrefix="partH"
    starterCode={starterCode}
>
    <!-- Intro Slot -->
    <p class="lesson-subtitle" slot="intro">
        Manage authentication tokens in React state and handle login/logout transitions.
    </p>

    <!-- Concept Slot (Left Pane) -->
    <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
        <!-- Unified Mission Card -->
        <article class="concept-card mission-card">
            <div class="mission-header">
                <h3><span>🎯</span> Your Mission</h3>
                <span class="checklist-counter" id="checklistCounter">0 / 2</span>
            </div>
            <div class="target-output-preview" style="background: #f1f5f9; border-radius: 8px; padding: 10px 12px; margin: 8px 0 12px 0; border: 1px dashed #94a3b8; font-size: 0.82rem;">
                <strong style="color: #334155;">🖼️ Target Goal:</strong>
                <div style="color: #475569; margin-top: 3px;">Build an authenticated login portal that stores auth credentials in state and renders the bearer session!</div>
            </div>
            <p class="mission-subtitle">Follow the step-by-step instructions below in the editor:</p>
            <div id="taskChecklist"></div>
        </article>

        <!-- Analogy Explainer Card -->
        <article class="concept-card">
            <h3><span>💡</span> Core Concept &amp; Mental Model</h3>
            <p>Authenticate user credentials with FastAPI backend, extract JWT bearer tokens, and persist tokens to browser storage.</p>
            <div class="analogy-callout">
                <strong>Real-World Analogy:</strong> A JWT login response is like receiving your digital keycard at a luxury hotel 🏨: you store it in your pocket (localStorage) to open authorized doors without giving your password again.
            </div>
        </article>
    </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
    import { DojoEngine } from '../../../scripts/dojo';

    function initLesson() {
        const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
        const lineNumbers = document.getElementById('ideLineNumbers');
        const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
        const previewIframe = document.getElementById('livePreviewIframe') as HTMLIFrameElement | null;
        const checkBtn = document.getElementById('checkAnswerBtn');
        const resetBtn = document.getElementById('resetEditorBtn');

        if (!editor) return;

        const DRAFT_KEY = 'novicodes_draft_partH_lesson1';
        const DEFAULT_STARTER = `// 🛡️ Level 9 Lesson 1: Stateless JWT Login & Token Storage!

function LoginPortal() {
  const [token, setToken] = React.useState(null);

  const handleLogin = (e) => {
    e.preventDefault();
    const fakeToken = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.demo";
    // 👉 STEP 1: Persist token to localStorage using localStorage.setItem("jwt_token", fakeToken):
    // (Hint: localStorage.setItem("jwt_token", fakeToken);)
    

    // 👉 STEP 2: Update local state with setToken(fakeToken):
    
  };

  return (
    <div style={{ padding: '24px', fontFamily: 'sans-serif', maxWidth: '400px', margin: '0 auto', background: '#ffffff', borderRadius: '16px', border: '1px solid #e2e8f0' }}>
      <h2>🔐 Secure Member Login</h2>
      {token ? (
        <div style={{ padding: '16px', background: '#dcfce7', color: '#15803d', borderRadius: '10px' }}>
          <strong>✅ Authenticated!</strong>
          <p style={{ fontSize: '0.80rem', margin: '6px 0 0', wordBreak: 'break-all' }}>Bearer: {token}</p>
        </div>
      ) : (
        <form onSubmit={handleLogin} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
          <input defaultValue="dev_user" placeholder="Username" style={{ padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }} />
          <input type="password" defaultValue="secret123" placeholder="Password" style={{ padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }} />
          <button type="submit" style={{ padding: '10px', background: '#4f46e5', color: 'white', border: 'none', borderRadius: '8px', fontWeight: 700, cursor: 'pointer' }}>
            Authenticate with JWT
          </button>
        </form>
      )}
    </div>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<LoginPortal />);
`;

        // 1. Initialize Real-Time Conceptual Checklist
        DojoEngine.initChecklist([
            { id: 'storage', label: 'Save token with <code>localStorage.setItem("jwt_token", ...)</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /localStorage\.setItem[\s\n]*\([\s\n]*["']jwt_token["']/i.test(c); } },
            { id: 'state', label: 'Update state with <code>setToken(...)</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /setToken[\s\n]*\(/i.test(c); } }
        ], {
            containerId: 'taskChecklist',
            mode: 'javascript'
        });

        // 2. Line numbers & scroll sync
        function syncScroll() {
            if (lineNumbersInner && editor) {
                lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
            }
        }

        function updateEditor() {
            if (!editor || !lineNumbersInner) return;
            const lines = editor.value.split('\n');
            const lineCount = Math.max(lines.length, 1);
            lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
            syncScroll();

            // Render live React JSX preview inside iframe
            if (previewIframe) {
                const code = editor.value;
                const htmlDoc = `<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/react@18/umd/react.development.js"><\/script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"><\/script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"><\/script>
  <style>
    body { margin: 0; padding: 12px; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #ffffff; color: #0f172a; }
  </style>
</head>
<body>
  <div id="root"></div>
  <script type="text/babel">
    try {
      ${code}
    } catch (err) {
      document.getElementById('root').innerHTML = '<div style="color:#ef4444; background:#fef2f2; border:1px solid #fecaca; padding:12px; border-radius:8px; font-size:13px; font-family:monospace;"><b>⚠️ Render Error:</b> ' + err.message + '</div>';
    }
  <\/script>
</body>
</html>`;
                previewIframe.srcdoc = htmlDoc;
            }

            // Real-Time Syntax & Diagnostics
            DojoEngine.lint(editor.value, 'javascript');

            // Real-Time Checklist Verification
            DojoEngine.runChecklist(editor.value);
        }

        // 3. Draft Auto-Save & Crash Resilience
        DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

        editor.addEventListener('scroll', syncScroll);

        editor.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                e.preventDefault();
                const start = editor.selectionStart;
                const end = editor.selectionEnd;
                editor.value = editor.value.substring(0, start) + '  ' + editor.value.substring(end);
                editor.selectionStart = editor.selectionEnd = start + 2;
                updateEditor();
            }
        });

        // 4. Safe Reset Button Handler
        if (resetBtn) {
            DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, () => {
                updateEditor();
            });
        }

        // 5. Check & Verify Code Action with Diagnostics
        if (checkBtn) {
            checkBtn.addEventListener('click', () => {
                DojoEngine.verifySubmission(editor.value, {
                    lessonTitle: 'Stateless JWT Login & Token Storage',
                    xp: 250,
                    completionKey: 'partH_lesson1_remake_complete',
                    nextUrl: '/8. partH/lesson2/lesson2_remake.html',
                    mode: 'javascript'
                });
            });
        }

        // Initial Paint
        updateEditor();
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLesson);
    } else {
        initLesson();
    }
</script>
