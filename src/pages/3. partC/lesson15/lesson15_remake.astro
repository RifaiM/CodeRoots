---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `// 🏆 Lesson 15: The Ultimate React Task Tracker Capstone!

function TaskTracker() {
  // 👉 STEP 1: Declare state 'tasks' initialized to ["Master React JSX", "Build Component Tree"]
  // (Hint: const [tasks, setTasks] = React.useState(["Master React JSX", "Build Component Tree"]);)
  const [tasks, setTasks] = React.useState([]);
  const [input, setInput] = React.useState("");

  const handleAddTask = (e) => {
    e.preventDefault();
    if (!input.trim()) return;
    // 👉 STEP 2: Append new task immutably: setTasks([...tasks, input]); setInput("");
    
  };

  return (
    <div style={{ padding: '24px', fontFamily: 'sans-serif', maxWidth: '440px', margin: '0 auto', background: '#ffffff', borderRadius: '16px', border: '1px solid #e2e8f0', boxShadow: '0 4px 12px rgba(0,0,0,0.05)' }}>
      <h2 style={{ color: '#0f172a', margin: '0 0 6px 0' }}>🏆 React Task Dojo</h2>
      <p style={{ color: '#64748b', fontSize: '0.90rem', margin: '0 0 16px 0' }}>Total active tasks: <strong>{tasks.length}</strong></p>

      <form onSubmit={handleAddTask} style={{ display: 'flex', gap: '8px', marginBottom: '16px' }}>
        <input 
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="New task title..."
          style={{ flex: 1, padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }}
        />
        <button type="submit" style={{ padding: '10px 16px', background: '#6366f1', color: 'white', border: 'none', borderRadius: '8px', cursor: 'pointer', fontWeight: 600 }}>
          Add
        </button>
      </form>

      <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
        {tasks.map((task, idx) => (
          <li key={idx} style={{ padding: '10px 12px', background: '#f8fafc', borderRadius: '8px', marginBottom: '6px', border: '1px solid #e2e8f0' }}>
            ✅ {task}
          </li>
        ))}
      </ul>
    </div>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<TaskTracker />);
`;
const level5Lessons = [
    {
        "id": 1,
        "title": "ES6+ Powers: Spread & Destructuring",
        "url": "/3. partC/lesson1/lesson1_remake.html"
    },
    {
        "id": 2,
        "title": "React Component Tree",
        "url": "/3. partC/lesson2/lesson2_remake.html"
    },
    {
        "id": 3,
        "title": "Dynamic Lists with .map()",
        "url": "/3. partC/lesson3/lesson3_remake.html"
    },
    {
        "id": 4,
        "title": "Passing Props to Components",
        "url": "/3. partC/lesson4/lesson4_remake.html"
    },
    {
        "id": 5,
        "title": "Interactive State with useState()",
        "url": "/3. partC/lesson5/lesson5_remake.html"
    },
    {
        "id": 6,
        "title": "Real-Time Input Binding",
        "url": "/3. partC/lesson6/lesson6_remake.html"
    },
    {
        "id": 7,
        "title": "Effects with useEffect()",
        "url": "/3. partC/lesson7/lesson7_remake.html"
    },
    {
        "id": 8,
        "title": "Component Slots with children",
        "url": "/3. partC/lesson8/lesson8_remake.html"
    },
    {
        "id": 9,
        "title": "Lifting State Up",
        "url": "/3. partC/lesson9/lesson9_remake.html"
    },
    {
        "id": 10,
        "title": "Conditional UI with Ternaries",
        "url": "/3. partC/lesson10/lesson10_remake.html"
    },
    {
        "id": 11,
        "title": "Multi-Input Forms with Dynamic Keys",
        "url": "/3. partC/lesson11/lesson11_remake.html"
    },
    {
        "id": 12,
        "title": "Loading Guards & Fallback UIs",
        "url": "/3. partC/lesson12/lesson12_remake.html"
    },
    {
        "id": 13,
        "title": "Fetching Live REST APIs",
        "url": "/3. partC/lesson13/lesson13_remake.html"
    },
    {
        "id": 14,
        "title": "Debounced Search & Cleanup",
        "url": "/3. partC/lesson14/lesson14_remake.html"
    },
    {
        "id": 15,
        "title": "React Capstone Mini-App",
        "url": "/3. partC/lesson15/lesson15_remake.html"
    }
];
---

<LessonLayout
    title="Project 15: React Capstone App | Level 5 React Dojo"
    description="Combine state, dynamic lists, controlled inputs, and component decomposition into a complete, interactive React Task Tracker!"
    levelTag="Level 5 • Framework Dojo"
    lessonNum={15}
    lessonTitle="React Capstone Mini-App"
    xpAmount={150}
    completionKey="partC_lesson15_remake_complete"
    nextLessonUrl="/3. partC/certificate.html"
    hubUrl="/3. partC/hub.html"
    hubTitle="Level 5 Hub"
    hubIcon="⚛️"
    fileTab="App.jsx"
    starterCode={starterCode}
    mode="html"
    jumpLessons={level5Lessons}
    certificateUrl="/3. partC/certificate.html"
    trackPrefix="partC"
>
    <!-- Intro Slot -->
    <p class="lesson-subtitle" slot="intro">
        Build a production-grade single page React application combining state, props, lifecycle effects, and API integration.
    </p>

    <!-- Concept Slot (Left Pane) -->
    <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
        
        <!-- 🎯 PROGATE-STYLE TOP MISSION CARD: Immediately informs learner what to do -->
        <!-- Unified Mission Card -->
        <article class="concept-card mission-card">
            <div class="mission-header">
                <h3><span>🎯</span> Your Mission</h3>
                <span class="checklist-counter" id="checklistCounter">0 / 2</span>
            </div>
            <div class="target-output-preview" style="background: #f1f5f9; border-radius: 8px; padding: 10px 12px; margin: 8px 0 12px 0; border: 1px dashed #94a3b8; font-size: 0.82rem;">
                <strong style="color: #334155;">🖼️ Target Goal:</strong>
                <div style="color: #475569; margin-top: 3px;">A complete, production-ready React Task Manager where you can add new todos and see the task count update live!</div>
            </div>
            <p class="mission-subtitle">Follow the step-by-step instructions below in the editor:</p>
            <div id="taskChecklist"></div>
        </article>
        
        <!-- Core Concept & Analogy -->
        <article class="concept-card">
            <h3><span>💡</span> Core Concept &amp; Mental Model</h3>
            <div class="analogy-callout">
                <strong>Real-World Analogy:</strong> Building your capstone is like assembling your own dream racecar 🏎️ using all the precision engine components and tools you mastered throughout Level 5!
            </div>
        </article>

        <!-- Interactive Feature Explorer -->
        <article class="concept-card">
            <h3><span>⚛️</span> Fullstack React App Architecture</h3>
            <p style="color: #475569; font-size: 0.88rem; margin: 0 0 10px 0;">Key syntax building blocks and architecture patterns:</p>
            <div class="tag-demo">
                <div class="tag-example">
                    <div class="tag-code">{"const [tasks, setTasks] = useState([...])"}</div>
                    <div class="tag-result">{"Main collection state for data records"}</div>
                </div>
                <div class="tag-example">
                    <div class="tag-code">{"setTasks([...tasks, newTask])"}</div>
                    <div class="tag-result">{"Adds items immutably without direct mutation"}</div>
                </div>
                <div class="tag-example">
                    <div class="tag-code">{"tasks.filter(t => t.id !== id)"}</div>
                    <div class="tag-result">{"Deletes item by filtering out matched ID"}</div>
                </div>
            </div>
        </article>

        <!-- Working Reference Code & Architecture Snippet -->
        <article class="concept-card">
            <h3><span>💻</span> Reference Architecture Pattern</h3>
            <p style="color: #475569; font-size: 0.88rem; margin: 0 0 8px 0;">How this pattern is structured in production React applications:</p>
            <div class="code-snippet-box">
                <button class="snippet-copy-btn" onclick="navigator.clipboard.writeText(document.getElementById('refCodeBox_15')?.innerText || '')">📋 Copy</button>
                <pre><code id="refCodeBox_15">{"function TaskManager() {\n  const [tasks, setTasks] = React.useState([\n    { id: 1, title: \"Master React Hooks\", done: true },\n    { id: 2, title: \"Build Capstone SPA\", done: false }\n  ]);\n  const [input, setInput] = React.useState(\"\");\n\n  const handleAdd = () => {\n    if (!input.trim()) return;\n    setTasks([...tasks, { id: Date.now(), title: input, done: false }]);\n    setInput(\"\");\n  };\n\n  return (\n    <div>\n      <input value={input} onChange={e => setInput(e.target.value)} />\n      <button onClick={handleAdd}>Add Task</button>\n      <ul>{tasks.map(t => <li key={t.id}>{t.title}</li>)}</ul>\n    </div>\n  );\n}"}</code></pre>
            </div>
        </article>

        

    </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
    import { DojoEngine } from '../../../scripts/dojo';

    function initLesson() {
        const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
        const lineNumbers = document.getElementById('ideLineNumbers');
        const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
        const previewIframe = document.getElementById('livePreviewIframe') as HTMLIFrameElement | null;
        const checkBtn = document.getElementById('checkAnswerBtn');
        const resetBtn = document.getElementById('resetEditorBtn');

        if (!editor) return;

        const DRAFT_KEY = 'novicodes_draft_partC_lesson15';
        const DEFAULT_STARTER = `// 🏆 Lesson 15: The Ultimate React Task Tracker Capstone!

function TaskTracker() {
  // 👉 STEP 1: Declare state 'tasks' initialized to ["Master React JSX", "Build Component Tree"]
  // (Hint: const [tasks, setTasks] = React.useState(["Master React JSX", "Build Component Tree"]);)
  const [tasks, setTasks] = React.useState([]);
  const [input, setInput] = React.useState("");

  const handleAddTask = (e) => {
    e.preventDefault();
    if (!input.trim()) return;
    // 👉 STEP 2: Append new task immutably: setTasks([...tasks, input]); setInput("");
    
  };

  return (
    <div style={{ padding: '24px', fontFamily: 'sans-serif', maxWidth: '440px', margin: '0 auto', background: '#ffffff', borderRadius: '16px', border: '1px solid #e2e8f0', boxShadow: '0 4px 12px rgba(0,0,0,0.05)' }}>
      <h2 style={{ color: '#0f172a', margin: '0 0 6px 0' }}>🏆 React Task Dojo</h2>
      <p style={{ color: '#64748b', fontSize: '0.90rem', margin: '0 0 16px 0' }}>Total active tasks: <strong>{tasks.length}</strong></p>

      <form onSubmit={handleAddTask} style={{ display: 'flex', gap: '8px', marginBottom: '16px' }}>
        <input 
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="New task title..."
          style={{ flex: 1, padding: '10px', borderRadius: '8px', border: '1px solid #cbd5e1' }}
        />
        <button type="submit" style={{ padding: '10px 16px', background: '#6366f1', color: 'white', border: 'none', borderRadius: '8px', cursor: 'pointer', fontWeight: 600 }}>
          Add
        </button>
      </form>

      <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
        {tasks.map((task, idx) => (
          <li key={idx} style={{ padding: '10px 12px', background: '#f8fafc', borderRadius: '8px', marginBottom: '6px', border: '1px solid #e2e8f0' }}>
            ✅ {task}
          </li>
        ))}
      </ul>
    </div>
  );
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<TaskTracker />);
`;

        // 1. Initialize Real-Time Conceptual Checklist
        DojoEngine.initChecklist([
            { id: 'tasks_state', label: 'Declare tasks state with <code>React.useState(...)</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /const\s*\[\s*tasks\s*,\s*setTasks\s*\]\s*=\s*(?:React\.)?useState/i.test(c); } },
            { id: 'add_task', label: 'Append new task immutably with <code>setTasks([...tasks, input])</code>', fn: (code) => { const c = code.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(?<!:)\/\/[^\r\n]*/g, ''); return /setTasks\s*\(\s*\[\s*\.\.\.tasks\s*,\s*input\s*\]\s*\)/i.test(c) || /setTasks\s*\(\s*\(?t\)?\s*=>\s*\[\s*\.\.\.t/i.test(c); } }
        ], {
            containerId: 'taskChecklist',
            mode: 'javascript'
        });

        // 2. Line numbers & scroll sync
        function syncScroll() {
            if (lineNumbersInner && editor) {
                lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
            }
        }

        function updateEditor() {
            if (!editor || !lineNumbersInner) return;
            const lines = editor.value.split('\n');
            const lineCount = Math.max(lines.length, 1);
            lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
            syncScroll();

            // Render live React JSX preview inside iframe
            if (previewIframe) {
                const code = editor.value;
                const htmlDoc = `<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/react@18/umd/react.development.js"><\/script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"><\/script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"><\/script>
  <style>
    body { margin: 0; padding: 12px; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #ffffff; color: #0f172a; }
  </style>
</head>
<body>
  <div id="root"></div>
  <script type="text/babel">
    try {
      ${code}
    } catch (err) {
      document.getElementById('root').innerHTML = '<div style="color:#ef4444; background:#fef2f2; border:1px solid #fecaca; padding:12px; border-radius:8px; font-size:13px; font-family:monospace;"><b>⚠️ Render Error:</b> ' + err.message + '</div>';
    }
  <\/script>
</body>
</html>`;
                previewIframe.srcdoc = htmlDoc;
            }

            // Real-Time Checklist Verification
            // Real-Time JavaScript & React Diagnostics
            DojoEngine.lint(editor.value, 'javascript');

            // Real-Time Checklist Verification
            DojoEngine.runChecklist(editor.value);
        }

        // 3. Draft Auto-Save & Crash Resilience
        DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

        editor.addEventListener('scroll', syncScroll);

        editor.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                e.preventDefault();
                const start = editor.selectionStart;
                const end = editor.selectionEnd;
                editor.value = editor.value.substring(0, start) + '  ' + editor.value.substring(end);
                editor.selectionStart = editor.selectionEnd = start + 2;
                updateEditor();
            }
        });

        // 4. Safe Reset Button Handler
        if (resetBtn) {
            DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, () => {
                updateEditor();
            });
        }

        // Initial Paint
        updateEditor();
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLesson);
    } else {
        initLesson();
    }
</script>
