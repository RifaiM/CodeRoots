---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `# 🛠️ Level 7A Lesson 1: Git & GitHub Workflow Practice

# Step 1: Initialize Git repository
# Write your initialize command here:


# Step 2: Create a .gitignore file ignoring node_modules/ and .env
# Write your file creation command here:


# Step 3: Stage all files
# Write your stage command here:


# Step 4: Create initial commit
# Write your commit command here:


# Step 5: Push to remote main branch
# Write your push command here:
`;

const branchLessons = [
    {
        "id": 1,
        "title": "Git & GitHub Workflow",
        "url": "/6. partF/branchA/lesson1_remake.html"
    },
    {
        "id": 2,
        "title": "Vercel & Netlify Deploy",
        "url": "/6. partF/branchA/lesson2_remake.html"
    },
    {
        "id": 3,
        "title": "Cloudflare Pages & Edge",
        "url": "/6. partF/branchA/lesson3_remake.html"
    },
    {
        "id": 4,
        "title": "Render & Fly.io Deploy",
        "url": "/6. partF/branchA/lesson4_remake.html"
    },
    {
        "id": 5,
        "title": "FastAPI CORS Security",
        "url": "/6. partF/branchA/lesson5_remake.html"
    },
    {
        "id": 6,
        "title": "GitHub Actions CI/CD",
        "url": "/6. partF/branchA/lesson6_remake.html"
    }
];
---

<LessonLayout
    title="Milestone 01: Git & GitHub Workflow | Level 7A • Cloud & DevOps"
    description="Master local Git version control, staging, atomic commits, branch renaming, and pushing to GitHub remotes."
    levelTag="Level 7A • Cloud & DevOps"
    lessonNum={1}
    lessonTitle="Git & GitHub Workflow"
    xpAmount={250}
    completionKey="partF_branchA_lesson1_complete"
    nextLessonUrl="/6. partF/branchA/lesson2_remake.html"
    hubUrl="/6. partF/hub.html"
    hubTitle="Level 7 Hub"
    hubIcon="🚀"
    fileTab="terminal.sh"
    starterCode={starterCode}
    mode="terminal"
    jumpLessons={branchLessons}
    certificateUrl="/6. partF/certificate.html"
    trackPrefix="partF_branchA"
>
    <!-- Intro Slot -->
    <p class="lesson-subtitle" slot="intro">
        Master local Git version control, staging, atomic commits, branch renaming, and pushing to GitHub remotes.
    </p>

    <!-- Concept Slot (Left Pane) -->
    <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
        
        <!-- Core Concept & Analogy -->
        <article class="concept-card">
            <h3><span>💡</span> Core Architecture &amp; Mental Model</h3>
            <div class="analogy-callout">
                <strong>Real-World Analogy:</strong> Git is a time-machine for your code: commits create immutable save-points you can travel back to anytime, while GitHub is the cloud bank storing your timeline.
            </div>
        </article>

        
            <article class="concept-card">
                <h3><span>🌳</span> Git Stage Lifecycle</h3>
                <div class="tag-demo">
                    <div class="tag-example">
                        <div class="tag-code">git add .</div>
                        <div class="tag-result">Working Directory → Staging Area</div>
                    </div>
                    <div class="tag-example">
                        <div class="tag-code">git commit -m "..."</div>
                        <div class="tag-result">Staging Area → Local Repository</div>
                    </div>
                    <div class="tag-example">
                        <div class="tag-code">git push origin main</div>
                        <div class="tag-result">Local Repository → Remote GitHub Cloud</div>
                    </div>
                </div>
            </article>

        <!-- Working Reference Code & Target Result -->
        <article class="concept-card">
            <h3><span>💻</span> Reference Pattern</h3>
            <p style="color: #475569; font-size: 0.88rem; margin: 0 0 8px 0;">Production architecture specification:</p>
            <div class="code-snippet-box">
                <button class="snippet-copy-btn" type="button" aria-label="Copy code snippet">📋 Copy</button>
                <pre><code id="refCodeBox_branchA_1">git init
echo "node_modules/" &gt; .gitignore
echo ".env" &gt;&gt; .gitignore
git add .
git commit -m "feat: initial commit"
git branch -M main
git push -u origin main</code></pre>
            </div>
        </article>

        <!-- Unified Mission Card -->
        <article class="concept-card mission-card">
            <div class="mission-header">
                <h3><span>🎯</span> Your Mission</h3>
                <span class="checklist-counter" id="checklistCounter">0 / 2</span>
            </div>
            <p class="mission-subtitle">Complete all milestone goals below in the editor:</p>
            <div id="taskChecklist"></div>
        </article>

    </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
    import { DojoEngine } from '../../../scripts/dojo';

    function initLesson() {
        const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
        const lineNumbers = document.getElementById('ideLineNumbers');
        const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
        const terminalScreen = document.getElementById('terminalScreen');
        const previewIframe = document.getElementById('livePreviewIframe') as HTMLIFrameElement | null;
        const checkBtn = document.getElementById('checkAnswerBtn');
        const resetBtn = document.getElementById('resetEditorBtn');

        if (!editor) return;

        const DRAFT_KEY = 'novicodes_draft_partF_branchA_lesson1';
        const DEFAULT_STARTER = `# 🛠️ Level 7A Lesson 1: Git & GitHub Workflow Practice

# Step 1: Initialize Git repository
# Write your initialize command here:


# Step 2: Create a .gitignore file ignoring node_modules/ and .env
# Write your file creation command here:


# Step 3: Stage all files
# Write your stage command here:


# Step 4: Create initial commit
# Write your commit command here:


# Step 5: Push to remote main branch
# Write your push command here:
`;

        // 1. Initialize Real-Time Conceptual Checklist
        DojoEngine.initChecklist([
                { id: 'init', label: 'Run <code>git init</code>', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /git\s+init/i.test(c); } },
                { id: 'ignore', label: 'Create <code>.gitignore</code>', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /\.gitignore/i.test(c); } },
                { id: 'add', label: 'Stage files with <code>git add .</code>', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /git\s+add/i.test(c); } },
                { id: 'commit', label: 'Commit with <code>git commit -m</code>', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /git\s+commit\s+-m/i.test(c); } },
                { id: 'push', label: 'Push to remote with <code>git push</code>', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /git\s+push/i.test(c); } }
            ], {
            containerId: 'taskChecklist',
            submitBtnId: 'checkAnswerBtn',
            nextBtnId: 'nextLessonBtn',
            mode: 'terminal'
        });

        // 2. Line numbers & scroll sync
        function syncScroll() {
            if (lineNumbersInner && editor) {
                lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
            }
        }

        function updateEditor() {
            if (!editor || !lineNumbersInner) return;
            const lines = editor.value.split('\n');
            const lineCount = Math.max(lines.length, 1);
            lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
            syncScroll();

            // Preview update if HTML mode
            if (previewIframe) {
                previewIframe.srcdoc = `<!DOCTYPE html><html><body style="font-family:sans-serif;padding:16px;">${editor.value}</body></html>`;
            }

            // Real-Time Checklist Verification
            // Real-Time Syntax & Diagnostics
            DojoEngine.lint(editor.value, 'javascript');

            // Real-Time Checklist Verification
            DojoEngine.runChecklist(editor.value);
        }

        // 3. Draft Auto-Save & Crash Resilience
        DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

        editor.addEventListener('scroll', syncScroll);

        editor.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                e.preventDefault();
                const start = editor.selectionStart;
                const end = editor.selectionEnd;
                editor.value = editor.value.substring(0, start) + '  ' + editor.value.substring(end);
                editor.selectionStart = editor.selectionEnd = start + 2;
                updateEditor();
            }
        });

        // 4. Safe Reset Button Handler
        if (resetBtn) {
            DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, () => {
                updateEditor();
                if (terminalScreen) {
                    terminalScreen.innerHTML = '<div class="terminal-prompt" style="color: #94a3b8;">&gt; Terminal reset. Ready for input.</div>';
                }
            });
        }

        // 5. Terminal output simulation
        function simulateTerminal(code: string) {
            if (!terminalScreen) return;
            terminalScreen.innerHTML = `<div style="color: #34d399;">&gt; Executing milestone command stream...</div>
<div style="color: #f8fafc; font-family: monospace; font-size: 0.78rem; margin: 6px 0;">${code.replace(/\n/g, '<br/>')}</div>
<div style="color: #38bdf8; font-size: 0.76rem;">✓ [Command batch executed with exit code 0]</div>`;
        }

        // 6. Check & Verify Code Action with Intelligent Diagnostics
        if (checkBtn) {
            checkBtn.addEventListener('click', () => {
                if (typeof simulateTerminal === 'function') {
                    simulateTerminal(editor.value);
                }
                DojoEngine.verifySubmission(editor.value, {
                    lessonTitle: 'Git & GitHub Workflow',
                    xp: 250,
                    completionKey: 'partF_branchA_lesson1_complete',
                    nextUrl: '/6. partF/branchA/lesson2_remake.html',
                    mode: 'javascript'
                });
            });
        }

        // Initial render
        updateEditor();
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLesson);
    } else {
        initLesson();
    }
</script>
