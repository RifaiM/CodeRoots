---
import LessonLayout from '../../../layouts/LessonLayout.astro';

const starterCode = `# 🛠️ Level 6 Lesson 2 Practice Task: Control Flow & Logic Engines

user_role = "admin"
user_xp = 1200

# Step 1: Write an if/elif/else block checking user_role and user_xp
# If user_role == "admin" and user_xp >= 1000, print "Access Granted: Admin"
# Else, print "Access Restricted"
# Write your if/else block here:


# Step 2: Use a for loop with range(3) to print countdown or iteration count
# Write your for loop here:
`;

const level6Lessons = [
    {
        "id": 1,
        "title": "Python Essentials & Syntax",
        "url": "/5. partE/lesson1/lesson1_remake.html"
    },
    {
        "id": 2,
        "title": "Control Flow & Logic Engines",
        "url": "/5. partE/lesson2/lesson2_remake.html"
    },
    {
        "id": 3,
        "title": "Functions, Scope & Lambdas",
        "url": "/5. partE/lesson3/lesson3_remake.html"
    },
    {
        "id": 4,
        "title": "Data Structures & List Comprehensions",
        "url": "/5. partE/lesson4/lesson4_remake.html"
    },
    {
        "id": 5,
        "title": "Object-Oriented Programming (OOP)",
        "url": "/5. partE/lesson5/lesson5_remake.html"
    },
    {
        "id": 6,
        "title": "OOP Inheritance & Polymorphism",
        "url": "/5. partE/lesson6/lesson6_remake.html"
    },
    {
        "id": 7,
        "title": "Error & Exception Handling",
        "url": "/5. partE/lesson7/lesson7_remake.html"
    },
    {
        "id": 8,
        "title": "Modules, Packages & Standard Library",
        "url": "/5. partE/lesson8/lesson8_remake.html"
    },
    {
        "id": 9,
        "title": "File I/O & Data Persistence",
        "url": "/5. partE/lesson9/lesson9_remake.html"
    },
    {
        "id": 10,
        "title": "Asynchronous Python & Asyncio",
        "url": "/5. partE/lesson10/lesson10_remake.html"
    },
    {
        "id": 11,
        "title": "Databases & SQL ORM Integration",
        "url": "/5. partE/lesson11/lesson11_remake.html"
    },
    {
        "id": 12,
        "title": "RESTful Web APIs with FastAPI",
        "url": "/5. partE/lesson12/lesson12_remake.html"
    },
    {
        "id": 13,
        "title": "Authentication & Security Basics",
        "url": "/5. partE/lesson13/lesson13_remake.html"
    },
    {
        "id": 14,
        "title": "Guided Mini Project: Task Manager API",
        "url": "/5. partE/lesson14/lesson14_remake.html"
    },
    {
        "id": 15,
        "title": "🏆 Capstone Python Backend Service",
        "url": "/5. partE/lesson15/lesson15_remake.html"
    }
];
---

<LessonLayout
    title="Project 02: Control Flow & Logic Engines | Level 6 Python Dojo"
    description="Master if/elif/else decision structures, boolean operators, and range-based for loops in Python."
    levelTag="Level 6 • Python Dojo"
    lessonNum={2}
    lessonTitle="Control Flow & Logic Engines"
    xpAmount={150}
    completionKey="partE_lesson2_remake_complete"
    nextLessonUrl="/5. partE/lesson3/lesson3_remake.html"
    hubUrl="/5. partE/hub.html"
    hubTitle="Level 6 Hub"
    hubIcon="🐍"
    fileTab="main.py"
    starterCode={starterCode}
    mode="python"
    jumpLessons={level6Lessons}
    certificateUrl="/5. partE/certificate.html"
    trackPrefix="partE"
>
    <!-- Intro Slot -->
    <p class="lesson-subtitle" slot="intro">
        Master if/elif/else decision structures, boolean operators, and range-based for loops in Python.
    </p>

    <!-- Concept Slot (Left Pane) -->
    <div slot="concept" style="display: flex; flex-direction: column; gap: 16px;">
        
        <!-- Core Concept & Analogy -->
        <article class="concept-card">
            <h3><span>💡</span> Core Concept &amp; Mental Model</h3>
            <div class="analogy-callout">
                <strong>Real-World Analogy:</strong> Control flow statements act like railway switches on a train track: directing the execution path based on boolean true/false conditions.
            </div>
        </article>

        
        <article class="concept-card">
            <h3><span>🔀</span> Python Decision Logic</h3>
            <div class="tag-demo">
                <div class="tag-example">
                    <div class="tag-code">if condition:</div>
                    <div class="tag-result">Execute indented block if condition is True</div>
                </div>
                <div class="tag-example">
                    <div class="tag-code">for i in range(n):</div>
                    <div class="tag-result">Iterate cleanly from 0 up to n - 1</div>
                </div>
            </div>
        </article>

        <!-- Working Reference Code & Target Result -->
        <article class="concept-card">
            <h3><span>💻</span> Reference Code Pattern</h3>
            <p style="color: #475569; font-size: 0.88rem; margin: 0 0 8px 0;">How this architecture looks in real-world code:</p>
            <div class="code-snippet-box">
                <button class="snippet-copy-btn" type="button" aria-label="Copy code snippet">📋 Copy</button>
                <pre><code id="refCodeBox_2">user_role = "admin"
user_xp = 1200

if user_role == "admin" and user_xp &gt;= 1000:
    print("Access Granted: Admin")
else:
    print("Access Restricted")

for i in range(3):
    print(f"Iteration: &#123;i&#125;")</code></pre>
            </div>
        </article>

        <!-- Task Instructions Card -->
        <article class="concept-card" style="border-left: 4px solid #059669;">
            <h3><span>🎯</span> Your Hands-On Coding Goal</h3>
            <p style="color: #475569; font-size: 0.90rem; line-height: 1.6; margin: 0 0 10px 0;">
                In the code editor on the right pane:
            </p>
            <ol style="color: #334155; font-size: 0.90rem; line-height: 1.7; padding-left: 20px; margin: 0;">
                <li>Use <code>if</code> and <code>else</code> or <code>elif</code> with comparison operators.</li>
                <li>Use boolean logic like <code>and</code>, <code>==</code>, or <code>&gt;=</code>.</li>
                <li>Use a <code>for i in range(...)</code> loop.</li>
            </ol>
        </article>

    </div>
</LessonLayout>

<!-- Client-Side Scripts & Verification Engine -->
<script>
    import { DojoEngine } from '../../../scripts/dojo';

    function initLesson() {
        const editor = document.getElementById('lessonEditor') as HTMLTextAreaElement | null;
        const lineNumbers = document.getElementById('ideLineNumbers');
        const lineNumbersInner = document.getElementById('ideLineNumbersInner') || lineNumbers;
        const terminalScreen = document.getElementById('terminalScreen');
        const checkBtn = document.getElementById('checkAnswerBtn');
        const resetBtn = document.getElementById('resetEditorBtn');

        if (!editor) return;

        const DRAFT_KEY = 'novicodes_draft_partE_lesson2';
        const DEFAULT_STARTER = `# 🛠️ Level 6 Lesson 2 Practice Task: Control Flow & Logic Engines

user_role = "admin"
user_xp = 1200

# Step 1: Write an if/elif/else block checking user_role and user_xp
# If user_role == "admin" and user_xp >= 1000, print "Access Granted: Admin"
# Else, print "Access Restricted"
# Write your if/else block here:


# Step 2: Use a for loop with range(3) to print countdown or iteration count
# Write your for loop here:
`;

        // 1. Initialize Real-Time Conceptual Checklist
        DojoEngine.initChecklist([
            { id: 'if', label: 'Use <code>if</code> / <code>else</code> decision statements', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /if\s+[\s\S]+?:[\s\S]+else\s*:/i.test(c); } },
            { id: 'boolean', label: 'Use comparison / boolean logic (<code>==</code>, <code>and</code>)', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /==|and|>=/i.test(c); } },
            { id: 'for', label: 'Use <code>for ... in range(...)</code> loop', fn: (code) => { const c = code.replace(/#[^\n]*/g, ''); return /for\s+\w+\s+in\s+range\s*\(/i.test(c); } }
        ], {
            containerId: 'taskChecklist',
            submitBtnId: 'checkAnswerBtn',
            nextBtnId: 'nextLessonBtn',
            mode: 'python'
        });

        // 2. Line numbers & scroll sync
        function syncScroll() {
            if (lineNumbersInner && editor) {
                lineNumbersInner.style.transform = `translateY(-${editor.scrollTop}px)`;
            }
        }

        function updateEditor() {
            if (!editor || !lineNumbersInner) return;
            const lines = editor.value.split('\n');
            const lineCount = Math.max(lines.length, 1);
            lineNumbersInner.textContent = Array.from({ length: lineCount }, (_, i) => i + 1).join('\n');
            syncScroll();

            // Real-Time Checklist Verification
            DojoEngine.runChecklist(editor.value);
        }

        // 3. Draft Auto-Save & Crash Resilience
        DojoEngine.setupDraftPersistence(editor, DRAFT_KEY, DEFAULT_STARTER, updateEditor);

        editor.addEventListener('scroll', syncScroll);

        editor.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                e.preventDefault();
                const start = editor.selectionStart;
                const end = editor.selectionEnd;
                editor.value = editor.value.substring(0, start) + '    ' + editor.value.substring(end);
                editor.selectionStart = editor.selectionEnd = start + 4;
                updateEditor();
            }
        });

        // 4. Safe Reset Button Handler
        if (resetBtn) {
            DojoEngine.setupResetButton(resetBtn, editor, DRAFT_KEY, DEFAULT_STARTER, () => {
                updateEditor();
                if (terminalScreen) {
                    terminalScreen.innerHTML = '<div class="terminal-prompt" style="color: #94a3b8;">&gt; Python Terminal reset. Ready to execute main.py</div>';
                }
            });
        }

        // 5. Python Terminal Simulation & Verification
        function simulatePythonOutput(code: string) {
            if (!terminalScreen) return;
            const prints: string[] = [];
            const lines = code.split('\n');

            for (const line of lines) {
                const trimmed = line.trim();
                const printMatch = trimmed.match(/^prints*\((.*)\)$/);
                if (printMatch) {
                    let content = printMatch[1].trim();
                    // Strip outer quotes if simple string
                    if ((content.startsWith('"') && content.endsWith('"')) || (content.startsWith("'") && content.endsWith("'"))) {
                        content = content.slice(1, -1);
                    } else if (content.startsWith('f"') || content.startsWith("f'")) {
                        content = content.slice(2, -1);
                    }
                    prints.push(content);
                }
            }

            if (prints.length > 0) {
                terminalScreen.innerHTML = prints.map(p => `<div style="color: #34d399;">&gt; ${p}</div>`).join('') +
                    '<div style="color: #38bdf8; margin-top: 8px; font-size: 0.76rem;">✓ [Process exited with code 0]</div>';
            } else {
                terminalScreen.innerHTML = '<div style="color: #34d399;">&gt; main.py executed successfully!</div>' +
                    '<div style="color: #38bdf8; margin-top: 8px; font-size: 0.76rem;">✓ [Process exited with code 0]</div>';
            }
        }

        // 6. Check & Verify Code Action
        if (checkBtn) {
            checkBtn.addEventListener('click', () => {
                const code = editor.value;

                if (DojoEngine.isEmpty(code)) {
                    DojoEngine.showLint('Your code editor is empty! Write your Python code before verifying.', 'error', 'Follow the step-by-step instructions in the left pane.');
                    return;
                }

                simulatePythonOutput(code);

                const result = DojoEngine.runChecklist(code);

                if (result.allPassed) {
                    DojoEngine.clearLint();
                    DojoEngine.celebrateCompletion({
                        lessonTitle: 'Project 02: Control Flow & Logic Engines',
                        xp: 150,
                        completionKey: 'partE_lesson2_remake_complete',
                        nextUrl: '/5. partE/lesson3/lesson3_remake.html',
                        customMessage: 'You have mastered this Python backend challenge!'
                    });
                } else {
                    DojoEngine.showLint('Project requirements incomplete', 'warning', 'Check the task checklist below to see remaining requirements.');
                }
            });
        }

        // Initial render
        updateEditor();
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLesson);
    } else {
        initLesson();
    }
</script>
